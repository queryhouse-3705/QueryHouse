CREATE TEMPORARY TABLE t0 (c0 INTEGER, c1 VARCHAR(20), c2 FLOAT); INSERT INTO t0 (c0, c1, c2) VALUES (1, 'v0', 1.1), (2, 'v1', 2.2), (-3, 'v2', -3.3); SELECT c1, RANK() OVER (ORDER BY ABS(c2) DESC) AS rank_order FROM t0 WHERE c2 < 0;