CREATE TABLE t0 (c0 INTEGER, c1 FLOAT, c2 VARCHAR(10)); INSERT INTO t0 (c0, c1, c2) VALUES (1, 10.0, 'A'), (2, -5.0, 'B'), (3, 0.0, 'A'), (4, 100.0, 'B'), (5, -50.0, 'C'); SELECT c0, c1, c2, CUME_DIST() OVER (PARTITION BY CASE WHEN c2 = 'A' THEN 1 WHEN c2 = 'B' THEN 2 ELSE 3 END ORDER BY c1 ASC) AS dist FROM t0;