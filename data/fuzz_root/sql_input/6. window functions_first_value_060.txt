CREATE TABLE t0 (c0 INTEGER, c1 FLOAT, c2 VARCHAR(10)); INSERT INTO t0 (c0, c1, c2) VALUES (1, 10.0, 'A'), (2, 15.0, 'A'), (3, 20.0, 'B'), (4, 5.0, 'B'), (5, 30.0, 'C'); SELECT c0, c1, c2, FIRST_VALUE(c1) OVER (PARTITION BY c2 ORDER BY c1 DESC) AS highest_in_group, c1 / FIRST_VALUE(c1) OVER (PARTITION BY c2 ORDER BY c1 DESC) AS relative_value FROM t0;