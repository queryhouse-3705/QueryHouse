CREATE TABLE t0 (c0 INTEGER, c1 FLOAT, c2 VARCHAR(10)); INSERT INTO t0 (c0, c1, c2) VALUES (1, 1.0, 'alpha'), (2, -2.0, 'beta'), (3, 3.0, 'gamma'), (4, -4.0, 'delta'), (5, 5.0, 'epsilon'); SELECT c0, c1, c2, LAST_VALUE(CASE WHEN c1 > 0 THEN c1 * LENGTH(c2) ELSE c1 / LENGTH(c2) END) OVER (ORDER BY c2 ROWS BETWEEN 1 PRECEDING AND 1 FOLLOWING) AS last_value_conditional_split FROM t0;