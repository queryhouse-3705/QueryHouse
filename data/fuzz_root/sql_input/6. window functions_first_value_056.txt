CREATE TABLE t0 (c0 INTEGER, c1 FLOAT, c2 VARCHAR(10)); INSERT INTO t0 (c0, c1, c2) VALUES (1, 10.0, 'A'), (2, 20.0, 'B'), (3, -5.0, 'A'), (4, 15.0, 'C'), (5, 0.0, 'B'); SELECT c0, c1, c2, FIRST_VALUE(c1) OVER (PARTITION BY CASE WHEN c2 = 'A' THEN 1 WHEN c2 = 'B' THEN 2 ELSE 3 END ORDER BY c1 DESC) AS first_val FROM t0;