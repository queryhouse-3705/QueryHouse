CREATE TABLE t0 (c0 INTEGER, c1 FLOAT, c2 VARCHAR(10)); INSERT INTO t0 (c0, c1, c2) VALUES (1, 10.0, 'low'), (2, -20.0, 'medium'), (3, 30.0, 'high'), (4, -40.0, 'low'), (5, 50.0, 'medium'); SELECT c0, c1, c2, LEAD(CASE WHEN c2 = 'low' THEN c1 * LENGTH(c2) ELSE c1 / LENGTH(c2) END) OVER (PARTITION BY MOD(LENGTH(c2), 2) ORDER BY c0 DESC) AS lead_conditional_transform FROM t0;