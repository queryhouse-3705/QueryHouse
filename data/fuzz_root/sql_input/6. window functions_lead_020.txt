CREATE TABLE t0 (c0 INTEGER, c1 FLOAT, c2 VARCHAR(10)); INSERT INTO t0 (c0, c1, c2) VALUES (1, 100.0, 'low'), (2, -200.0, 'medium'), (3, 300.0, 'high'), (4, -400.0, 'low'), (5, 500.0, 'medium'); SELECT c0, c1, c2, LEAD(CASE WHEN c2 = 'low' THEN c1 * LENGTH(c2) ELSE c1 / LENGTH(c2) END) OVER (ORDER BY c1 DESC) AS lead_dynamic_sort FROM t0;