CREATE TABLE t0 (c0 INTEGER, c1 FLOAT, c2 VARCHAR(10)); INSERT INTO t0 (c0, c1, c2) VALUES (1, 5.0, 'alpha'), (2, -10.0, 'beta'), (3, 15.0, 'gamma'), (4, -20.0, 'delta'), (5, 25.0, 'epsilon'); SELECT c0, c1, c2, LAST_VALUE(c1 * LENGTH(c2) + CASE WHEN c2 LIKE 'a%' THEN 2 ELSE -1 END) OVER (PARTITION BY c2 ORDER BY c1 ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW) AS last_value_weighted_dynamic FROM t0;