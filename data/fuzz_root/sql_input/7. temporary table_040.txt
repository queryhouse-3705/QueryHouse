CREATE TEMPORARY TABLE t0 (c0 INTEGER, c1 VARCHAR(75), c2 FLOAT); INSERT INTO t0 (c0, c1, c2) VALUES (1, 'v0', 0.123), (2, 'v1', -0.456), (-3, 'v2', 0.789); SELECT FIRST_VALUE(c2) OVER (PARTITION BY c0 % 2 ORDER BY c2) AS first_in_partition FROM t0;